import {
  OPERAND_ADDED,
  OPERAND_SUBTRACTED,
  OPERAND_MULTIPLIED,
  OPERAND_DIVIDED
} from '../constants';

export default function sumReducer(state = 0, action) {
  // Write your reducer here.
  return state;
}
